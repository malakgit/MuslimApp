<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hamood.malak.muslimapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".owner_storage"></activity>
        <activity
            android:name=".MyUI.OwnerMainActivity"
            android:label="@string/title_activity_owner_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MyUI.SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyUI.AddPost" />
        <activity android:name=".MyUI.SignUp"></activity>


    </application>

</manifest>